import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'fc5f1e4d',
  appName: 'health',
  webDir: '../../dist/apps/health',
  bundledWebRuntime: false,
};

export default config;
